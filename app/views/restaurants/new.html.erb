<%= simple_form_for [@restaurant] do |r| %>
  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :category, multipleclass: "form-control" %>
  <%= r.submit "Add to list", class: "btn btn-primary" %>
<% end %>
